# Your Firebase project's settings.
fcm-demo.json

# The service account private key. It authorises sending to every device in the
# project, so it must never be committed.
service-account.json
*-service-account.json
